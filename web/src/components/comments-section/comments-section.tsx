import type { CommentsSectionProps } from './types'

export default function CommentsSection({ comments, totalComments }: CommentsSectionProps) {
  return (
    <div className="flex flex-col gap-4">
      <h1>COMMENTS SECTION</h1>
    </div>
  )
}
